// CompileTimeAspectPipeline.ExecuteAsync failed.
// Error LAMA0241 on `logger != null && logger.Value != null`: `Execution scope mismatch in the expression `logger != null && logger.Value != null`: the sub-expression `logger != null` is compile-time, but the other sub-expression `logger.Value != null` is run-time.`
